expression aggregate {
	level = error
	enabled = true
}

expression invalid {
	level = error
	enabled = true
}

alias {
	unexpected {
		level = error
		enabled = true
	}

	undefined {
		level = error
		enabled = true
	}

	duplicate {
		level = error
		enabled = true
	}

	missing {
		level = error
		enabled = true
	}
}
